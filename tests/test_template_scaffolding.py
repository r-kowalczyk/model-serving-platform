"""Tests for the initial template scaffolding.

This module contains a minimal placeholder test so that the template
repository has at least one passing test at creation time. It is
intended to be replaced or removed once real project tests are added.
The test defines no domain logic and only exercises the testing setup.
"""


# This placeholder test ensures that pytest and coverage succeed in a fresh clone so that the template is self-checking.
def test_template_scaffolding() -> None:
    """Verify that the template testing infrastructure is wired correctly.

    This test acts as a neutral placeholder which confirms that pytest,
    coverage and the surrounding tooling run without failure. It should
    be removed or replaced once real application tests are introduced.
    The assertion is intentionally trivial so that no domain behaviour
    is encoded here and only the infrastructure is exercised.
    """
    assert True
